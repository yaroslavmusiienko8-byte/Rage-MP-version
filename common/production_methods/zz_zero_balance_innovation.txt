# Zero Balance Mod: innovation system rework (part 1 of 2) via REPLACE_OR_CREATE (full overwrite).
# REPLACE_OR_CREATE: discards vanilla's whole definition, so the academia country_modifiers block
# (country_weekly_innovation_add) is REMOVED, not stacked. (INJECT was wrong here: for modifier
# blocks it ADDS to vanilla instead of replacing.)
# Universities no longer generate innovation directly; instead they contribute to
# literacy-growth-from-wealth (state_education_access_wealth_add), which feeds innovation INDIRECTLY
# via literacy. Values are on vanilla's scale: 0.0025 / 0.0035 / 0.005 (i.e. +0.25% / +0.35% / +0.5%
# education access per pop-wealth level). NOTE: these were briefly mis-set to 0.25/0.35/0.5 (+25%..+50%,
# a 100x error) and are now corrected. To justify the channel the methods are still expensive: paper
# input is 5x vanilla and total employment is raised to 1500/level. The country-wide wealth->literacy
# base now lives in law_public_schools (see common/laws/zz_zero_balance_education.txt).
# Vanilla building modifiers and pop-qualifications are reproduced; paper, employment and the
# wealth->education values are the Zero Balance changes. See static_modifiers part 2 for innovation.

REPLACE_OR_CREATE:pm_scholastic_education = {
	texture = "gfx/interface/icons/production_method_icons/scholastic_education.dds"

	building_modifiers = {
		workforce_scaled = {
			goods_input_paper_add = 25
		}

		level_scaled = {
			building_employment_clerks_add = 750
			building_employment_laborers_add = 750
		}
	}

	state_modifiers = {
		workforce_scaled = {
			state_pop_qualifications_mult = 0.1
			state_education_access_add = 0.03
		}
	}
}

REPLACE_OR_CREATE:pm_philosophy_department = {
	texture = "gfx/interface/icons/production_method_icons/philosophy_dept.dds"

	unlocking_technologies = {
		dialectics
	}

	building_modifiers = {
		workforce_scaled = {
			goods_input_paper_add = 50
		}

		level_scaled = {
			building_employment_clerks_add = 750
			building_employment_laborers_add = 750
		}
	}

	state_modifiers = {
		workforce_scaled = {
			state_pop_qualifications_mult = 0.15
			state_education_access_add = 0.05
		}
	}
}

REPLACE_OR_CREATE:pm_analytical_philosophy_department = {
	texture = "gfx/interface/icons/production_method_icons/analytical_philosophy_department.dds"

	unlocking_technologies = {
		analytical_philosophy
	}

	building_modifiers = {
		workforce_scaled = {
			goods_input_paper_add = 75
		}

		level_scaled = {
			building_employment_clerks_add = 600
			building_employment_laborers_add = 600
			building_employment_academics_add = 300
		}
	}

	state_modifiers = {
		workforce_scaled = {
			state_pop_qualifications_mult = 0.2
			state_education_access_add = 0.08
		}
	}
}
