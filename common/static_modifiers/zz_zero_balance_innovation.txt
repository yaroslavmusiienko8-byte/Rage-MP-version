# Zero Balance Mod: innovation system rework (part 2 of 2) via REPLACE_OR_CREATE (full overwrite).
# Innovation GENERATION is now tied to nationwide literacy. country_literacy_rate is a code static
# modifier whose values are scaled by the country's literacy rate (0-1), so country_weekly_innovation_add
# = 150 yields up to +150 weekly innovation at 100% literacy and proportionally less below that.
# This replaces the innovation universities used to generate (see production_methods part 1).
# Vanilla's icon, max_add and tech_spread are reproduced below unchanged; only innovation_add is new.
REPLACE_OR_CREATE:country_literacy_rate = {
	icon = gfx/interface/icons/timed_modifier_icons/modifier_lightbulb_positive.dds
	country_weekly_innovation_add = 150
	country_weekly_innovation_max_add = 150
	country_tech_spread_add = 75
}
